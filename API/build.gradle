repositories {
}

dependencies {
    implementation group: 'org.json', name: 'json', version: '20090211'
}

shadowJar {
    minimize()
}
jar.dependsOn shadowJar
logger.lifecycle("Building ResentAPI ${version}")